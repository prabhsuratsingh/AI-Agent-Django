[project]
name = "ai-agent-django"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.5",
    "jupyter>=1.1.1",
    "langgraph>=0.6.6",
    "permit>=2.7.5",
    "python-decouple>=3.8",
]
